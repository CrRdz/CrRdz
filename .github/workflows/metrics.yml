name: Metrics
on:
  schedule: [{cron: "0 */12 * * *"}]  # 每12小时自动更新
  workflow_dispatch:                   # 手动触发
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
